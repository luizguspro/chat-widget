{
  "name": "chat-widget-dialogflow",
  "version": "1.0.0",
  "description": "Sistema de Chat Widget configurável para Dialogflow CX",
  "scripts": {
    "install-all": "cd backend && npm install",
    "dev": "cd backend && npm run dev",
    "build": "cd backend && npm run build",
    "start": "cd backend && npm start"
  },
  "dependencies": {
    "@google-cloud/dialogflow-cx": "^5.3.0"
  }
}
